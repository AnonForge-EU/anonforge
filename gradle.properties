# AndroidX Migration
android.useAndroidX=true
# Jetifier (legacy support-library rewriting) not needed — all deps are AndroidX.
# Removed: was deprecated and slated for removal in AGP 10.

# Kotlin
kotlin.code.style=official

# Build Performance
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true